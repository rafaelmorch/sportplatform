type CoachRecommendationProps = {
  insight: string;
};

export default function CoachRecommendation({
  insight,
}: CoachRecommendationProps) {
  return (
    <section
      style={{
        position: "relative",
        overflow: "hidden",
        borderRadius: 20,
        border: "1.5px solid rgba(212,175,55,0.62)",
        background:
          "linear-gradient(145deg, rgba(32,32,35,0.98) 0%, rgba(11,11,13,0.99) 100%)",
        padding: "22px 20px",
        display: "grid",
        gap: 12,
        color: "#f8fafc",
        boxShadow:
          "0 20px 42px rgba(0,0,0,0.42), inset 0 1px 0 rgba(255,255,255,0.08), inset 0 -1px 0 rgba(96,66,15,0.34)",
      }}
    >
      <div
        aria-hidden="true"
        style={{
          position: "absolute",
          width: 180,
          height: 180,
          borderRadius: "50%",
          top: -115,
          right: -75,
          background:
            "radial-gradient(circle, rgba(212,175,55,0.16) 0%, rgba(212,175,55,0) 72%)",
          pointerEvents: "none",
        }}
      />

      <div
        style={{
          position: "relative",
          zIndex: 1,
          display: "flex",
          alignItems: "center",
          gap: 10,
        }}
      >
        <span
          style={{
            width: 30,
            height: 30,
            flexShrink: 0,
            borderRadius: "50%",
            display: "grid",
            placeItems: "center",
            color: "#09090b",
            background:
              "linear-gradient(145deg, #f2d178 0%, #bd8731 100%)",
            boxShadow:
              "0 7px 15px rgba(189,135,49,0.25), inset 0 1px 0 rgba(255,255,255,0.55)",
            fontSize: 15,
            fontWeight: 800,
          }}
        >
          ✦
        </span>

        <div
          style={{
            fontSize: 12,
            fontWeight: 700,
            textTransform: "uppercase",
            letterSpacing: "0.13em",
            color: "#e8cb76",
          }}
        >
          Recomendação do Coach
        </div>
      </div>

      <div
        style={{
          position: "relative",
          zIndex: 1,
          fontSize: "clamp(17px, 3vw, 20px)",
          fontWeight: 650,
          lineHeight: 1.5,
          letterSpacing: "-0.015em",
          color: "#ffffff",
        }}
      >
        {insight}
      </div>

      <div
        style={{
          position: "relative",
          zIndex: 1,
          fontSize: 12,
          lineHeight: 1.55,
          color: "#a1a1aa",
        }}
      >
        Orientação gerada a partir dos dados atualmente disponíveis na Sports
        Platform. Ela não substitui avaliação médica ou profissional.
      </div>

      <div
        aria-hidden="true"
        style={{
          position: "relative",
          zIndex: 1,
          width: 100,
          height: 3,
          marginTop: 4,
          borderRadius: 999,
          background:
            "linear-gradient(90deg, #bd8731 0%, #f1d27b 72%, rgba(241,210,123,0.05) 100%)",
          boxShadow: "0 2px 8px rgba(189,135,49,0.2)",
        }}
      />
    </section>
  );
}